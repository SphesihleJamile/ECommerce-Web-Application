@page
@model ECommerceWeb.Pages.Admin.MenuItemPages.IndexModel
@{
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Menu Item List
            </h2>
        </div>
        <div class="col-6">
            <a asp-page="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp; Create New Menu Item
            </a>
        </div>
    </div>

    <br /><br />

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Category</th>
                <th>Food Type</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @*Here we are going to be using DataTable.net from JQuery*@



            @*@foreach (var obj in Model.MenuItems)
            {
                <tr>
                    <td>@obj.Name</td>
                    <td>@obj.Decription</td>
                    <td>@obj.ImagePath</td>
                    <td>@obj.Price</td>
                    <td>@obj.FoodTypeId</td>
                    <td>@obj.CategoryId</td>
                    <td>
                        <a asp-page="Edit" asp-route-id="@obj.Id" class="btn btn-warning mx-2">
                            <i class="bi bi-pen"></i>
                        </a>
                        <a asp-page="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </td>
                </tr>
            }*@
        </tbody>
    </table>
</div>